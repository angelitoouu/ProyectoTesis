# Configuración del servidor Backend
HOST=localhost
PORT=3000
NODE_ENV=development

# Conexión a Base de Datos PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=postgres
PASSWORD=1234
DATABASE=TallerCostura

# Opcional: URL completa de conexión PostgreSQL (sobrescribe los campos anteriores si se define)
# DATABASE_URL=postgresql://postgres:1234@localhost:5432/TallerCostura
